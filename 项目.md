# 项目文档

## 一、 项目基本信息与技术栈

**软件名称：** VideoDownloader

**核心框架：** .NET SDK 9，结合 Avalonia UI 框架构建 Windows 桌面客户端。

**整体框架：** 采用 Clean Architecture + ReactiveUI 模式（解耦 UI、ViewModel、Service 与 Model）。

必须采用多项目（Multiple .csproj）内的整洁架构（Clean Architecture），使用四个项目建构项目，避免不同层级违规引用依赖：

| 项目 | 职责 | 引用 |
|---|---|---|
| VideoDownloader.Domain | 纯领域模型、服务接口、消息定义 | 无 |
| VideoDownloader.Application | 业务用例编排（下载/转码/依赖检测/URL分析/事件总线） | 仅 Domain |
| VideoDownloader.Infrastructure | 基础设施（进程管理/网络IO/文件系统/配置持久化/工具路径） | 仅 Domain |
| VideoDownloader（UI） | Avalonia 界面 + ViewModels | Application + Infrastructure |

严格遵循依赖倒置原则：内层代码不依赖外层代码，可以完全丢弃外部代码而不影响内部代码运行。

**下载扩展模块：** 采用插件化架构思想（定义 `ISiteDownloader` 接口，按网站拆分 .cs 文件，当前支持 YouTube / Bilibili / 通用）。

**跨模块通信：** 引入事件驱动 `EventMessenger`（`IEventMessenger`），用于下载完成自动通知 UI 或触发状态更新，避免 ViewModel 之间互相持有引用。消息定义（`Domain/Messenger/EventMessages.cs`）只保留有真实发送/订阅方的条目（曾清理 `DownloadTaskCreatedMessage` / `ConversionTaskCreatedMessage` 两条死消息）。

**依赖注入与 MVVM 规范：**
- 使用 `Microsoft.Extensions.DependencyInjection` 进行全局服务与 ViewModel 注册，**严格禁止在 ViewModel 或 Service 中直接使用 `new` 实例化其他服务**（注：子项 ViewModel 如 `DownloadItemViewModel` / `VideoConversionItemViewModel` 由父 ViewModel 创建组装，非服务，不在此限）。
- 使用 **ReactiveUI 23.2.28 + ReactiveUI.SourceGenerators** 简化属性与命令绑定（`[Reactive]` 生成属性、`[ReactiveCommand]` 生成命令、`ObservableAsPropertyHelper` 用于只读派生属性）；**禁止手写 INPC / ICommand / 事件处理器代码后置**。视图与 ViewModel 的纯 UI 交互（文件选择器、窗口关闭）通过 `ReactiveUI.Interaction` 实现。

**底层工具驱动：** yt-dlp（下载与元数据解析）与 ffmpeg（音视频转码与合并）。

**UI 项目 NuGet 依赖版本（VideoDownloader.csproj）：** Avalonia 12.1.1、Avalonia.Desktop 12.1.1、Avalonia.Themes.Fluent 12.1.1、Avalonia.Fonts.Inter 12.1.1、AvaloniaUI.DiagnosticsSupport 2.2.3、Microsoft.Extensions.DependencyInjection 10.0.10、ReactiveUI 23.2.28、ReactiveUI.Avalonia 12.0.2、ReactiveUI.SourceGenerators 3.2.0、Serilog 4.4.0、Serilog.Sinks.File 7.0.0、Svg.Controls.Skia.Avalonia 12.0.0.13。

**SVG 图标：** 使用 `Svg.Controls.Skia.Avalonia` 包渲染糖果渐变风格 SVG 图标，`<Image Source="{svg:SvgImage avares://...}" />` 加载。

**发布模式：** 单文件 exe 发布（`PublishSingleFile` + `SelfContained=true` + `PublishTrimmed` + `EnableCompressionInSingleFile`，约 24MB），已嵌入 .NET 9 运行时，目标机无需安装 .NET。发布后自动清理 PDB，产物仅剩单个 exe。除了外部依赖工具（.tools/ 目录）和生成的配置文件（config.json），不需要任何额外文件即可运行。发布版本目录从 `publish\v0.0.01` 开始按 `v0.0.02`、`v0.0.03` … 递增，每次任务结束自动发布。当前版本 v0.1.21。裁剪下已知 IL2026 警告均来自 ReactiveUI `WhenAnyValue` / `WhenActivated` 反射式 API（表达式树运行期查成员），其引用成员经 XAML 绑定等路径保留，历次裁剪产物实测运行正常，非真实缺陷。

**版本控制：** 已初始化 git 仓库（当前未做首次提交）。根目录 `.gitignore` 忽略：`bin/`、`obj/`、`publish/`、`.tools/`（运行时下载的 ffmpeg/ffprobe/yt-dlp 二进制，约 460MB）、`logs/`、`config.json`、`*.zip`、`*.user`、`.vs/`。

**日志：** `VideoDownloader.Domain.Logging.ILogger` 接口 + `VideoDownloader.Infrastructure.Logging.SerilogLogger` 实现 + DI 注入。所有被吞掉的异常通过 Serilog 记录到 `logs/app.log`（按日滚动，保留 30 天）。支持按模块（Download / Process / Config / FileSystem / Messenger / Startup）分组，可通过 Serilog 配置按模块拆分为独立日志文件。
- Serilog 在 `Program.Main` 入口**最先**配置（`App.ConfigureLogging()`，早于 Avalonia 与 DI 构建，最早期崩溃也能落盘），随后立即注册三个全局崩溃处理器（AppDomain / TaskScheduler / Dispatcher），统一走 `LogUnhandled`。崩溃路径显式 `new SerilogLogger()`（DI 可能不可用），为无状态薄包装、直写全局 `Serilog.Log.Logger`，输出与 DI 实例一致。日志级别设为 `Verbose`（全部输出）。
- 进程输出编码：中文 Windows 下 yt-dlp 输出 GBK（代码页 936），`ProcessRunner` 三处方法（`StartProcess`、`RunProcessAsync`、`RunProcessAndReadOutputAsync`）均使用 `Encoding.GetEncoding(936)`。`Program.Main` 中调用 `Encoding.RegisterProvider(CodePagesEncodingProvider.Instance)` 注册 GBK 编码支持（需 `System.Text.Encoding.CodePages` 包）。
- config.json 使用 System.Text.Json **源生成器**（`DownloadConfigurationJsonContext`，`JsonSourceGenerationOptions(WriteIndented = true)`）读写，兼容 `PublishTrimmed`；logger 经构造函数注入（`DownloadConfiguration.Load(ILogger)`），无静态 SetLogger。配置新增 `BypassProxyForBilibili`（默认 true）和 `BypassProxyForYoutube` 绕过代理设置。
- UI 项目显式引用 `Serilog` / `Serilog.Sinks.File`（App 层直接使用 LoggerConfiguration / WriteTo.File / RollingInterval）。

**SVG 图标：** 使用 `Svg.Controls.Skia.Avalonia` 包渲染 SVG 图标，`<Image Source="{svg:SvgImage avares://...}" />` 标记扩展加载。图标位于 `Assets/` 目录，采用 candy 渐变风格（`linearGradient` + `gradientUnits="userSpaceOnUse"`）。

---

## 二、 严格的文件与架构分离规范（硬性要求）

视图（View）、视图模型（ViewModel）、业务服务（Service）、数据模型（Model）必须隔离存放在不同的项目与目录中，绝不允许混在一个文件中。

### Models/ 文件夹（纯数据实体模型，位于 Domain/Models/）

- `VideoFormatModel.cs`：视频格式元数据实体（FormatId、分辨率、宽度/高度、视频编码、音频编码、HDR 标记、帧率、文件大小、是否仅音频等）
- `DownloadTaskModel.cs`：下载任务实体（URL、状态、进度、大小、清晰度列表等）
- `VideoConversionTaskModel.cs`：视频转码任务实体（输入/输出路径、容器、编码等）
- `DependencyStatusModel.cs` + `ToolStatus`：工具依赖状态实体（IsAvailable、Version、ExecutablePath）

### Extensions/ 文件夹（类型附加行为与静态辅助，位于 Domain/Extensions/）

- `VideoContainerExtensions.cs`：`AllContainers` 常量数组（Mp4/Mkv/WebM/Mov），供转码容器下拉框取枚举项。约定：为已有类型提供行为/常量整理的类放此目录，Models/ 只保留纯数据实体。

### Views/ 文件夹（仅存放 AXAML 视图，位于 UI/Views/ + UI 根目录）

- `MainWindow.axaml` + `.cs`：主窗口（位于项目根，非 Views/ 内；外壳 + 标签页切换）
- `DownloadView.axaml` + `.cs`：下载视图
- `VideoConversionView.axaml` + `.cs`：视频转码视图
- `DependencyView.axaml` + `.cs`：依赖工具检测与配置视图（嵌入到设置窗口内）
- `SettingsView.axaml` + `.cs`：系统设置窗口（网络代理 + 依赖检测）
- `AboutView.axaml` + `.cs`：关于窗口
- `MessageView.axaml` + `.cs`：提示消息弹窗（最小尺寸 150x100，文本与确定按钮居中）
- `FirstRunView.axaml` + `.cs`：首次使用提示窗口（使用提示；「知道了」勾选后才可确定；勾选「启动时不再显示」后下次启动跳过）

### ViewModels/ 文件夹（存放 UI 状态管理与命令绑定，位于 UI/ViewModels/）

- `MainWindowViewModel.cs`：主窗口 ViewModel（标签切换、状态栏、全局进度）
- `DownloadViewModel.cs` + `DownloadItemViewModel.cs`：下载页面及卡片 ViewModel
- `VideoConversionViewModel.cs` + `VideoConversionItemViewModel.cs`：视频转码页面及卡片 ViewModel
- `SettingsViewModel.cs`：系统设置 ViewModel（代理配置 + 网络测试 + 依赖检测）
- `DependencyViewModel.cs`：依赖检测 ViewModel（检查/下载/状态）

### Services/ 文件夹（存放底层驱动与纯 C# 业务逻辑）

**接口层（Domain/Services/）：**

| 接口 | 位置 | 用途 |
|---|---|---|
| `IDependencyService` | Domain/Services/Dependency/ | 依赖检测与下载 |
| `IProcessRunner` | Domain/Services/Process/ | 外部进程调用（支持并发控制、暂停、取消） |
| `IDownloadService` | Domain/Services/Download/ | 下载核心流程调度 |
| `ISiteDownloader` | Domain/Services/Download/ | 多网站策略模式接口 |
| `IVideoConversionService` | Domain/Services/Conversion/ | 视频转码服务 |
| `IVideoEncoderCatalog` | Domain/Services/Conversion/ | 编码器目录（映射容器与可用编码） |
| `IToolPathResolver` | Domain/Services/ | 工具路径解析 |
| `IDownloadConfiguration` | Domain/Services/ | 下载配置 |
| `ILogger` | Domain/Logging/ | 日志接口 |
| `IUrlAnalysisService` | Domain/Services/UI/ | URL 分析（播放列表检测） |
| `IFileCleanupService` | Domain/Services/FileSystem/ | 临时文件清理 |
| `INetworkTestService` | Domain/Services/Network/ | 网络连通测试 |
| `IEventMessenger` | Domain/Messenger/ | 跨模块事件总线 |

**实现层（Application/Services/ + Infrastructure/Services/）：**

| 实现 | 位置 | 用途 |
|---|---|---|
| `DownloadService` | Application/Services/Download/ | 播放列表解析、-F 格式解析、下载/暂停/续传 |
| `DependencyService` | Application/Services/Dependency/ | 依赖检测（调用 yt-dlp/ffmpeg 检查版本） |
| `VideoConversionService` | Application/Services/Conversion/ | 视频转码调度 |
| `UrlAnalysisService` | Application/Services/UI/ | URL 播放列表分析 |
| `EventMessenger` | Application/Messenger/ | 事件总线实现 |
| `DefaultDownloader` | Application/Services/Download/Sites/ | 通用网站适配器 |
| `YoutubeDownloader` | Application/Services/Download/Sites/ | YouTube 适配器 |
| `BilibiliDownloader` | Application/Services/Download/Sites/ | Bilibili 适配器 |
| `DownloadFormatHelpers` | Application/Services/Download/Sites/ | 视频+音频合并参数生成（按容器选最佳音频） |
| `SiteTitleResolver` | Application/Services/Download/Sites/ | yt-dlp --print title 取视频标题（20s 超时） |
| `SerilogLogger` | Infrastructure/Logging/ | 结构化日志（Serilog 实现） |
| `ProcessRunner` | Infrastructure/Services/Process/ | 外部进程启动、输出读取、暂停/终止 |
| `NetworkTestService` | Infrastructure/Services/Network/ | 网络连通测试 |
| `FileCleanupService` | Infrastructure/Services/FileSystem/ | 临时文件清理 |
| `DownloadConfiguration` | Infrastructure/Services/Download/ | config.json 持久化 |
| `ToolLocationProvider` | Infrastructure/Services/Dependency/ | 工具路径查找（.tools/ → PATH） |

**组合根（UI/App.axaml.cs）：** DI 容器在 `OnFrameworkInitializationCompleted` 内构建，`ServiceProvider` 为**局部变量**（无静态服务定位器 `App.Services`）；唯一不依赖容器的是崩溃日志路径 `LogUnhandled`（显式 `new SerilogLogger()`）。

**UI 层内部抽象（例外约定，不落 Domain）：** `IDialogService`（`src\VideoDownloader\Services\IDialogService.cs`）与其实现 `WindowService`（`src\VideoDownloader\Services\WindowService.cs`）均位于 **UI/Services/**。判定标准：该抽象仅被 UI 层消费、实现依赖 Avalonia `Window`/`View`（无法在 Infrastructure 落地），放 Domain 会成为空转抽象——接口归属由其消费层决定，跨层使用才放 Domain。

---

## 三、 核心模块详细功能规范

### 依赖检测与环境配置模块（DependencyService）

- 启动时自动检测（后台） + 设置窗口打开时自动检测（前台）。
- 检测顺序：应用运行目录 `.tools/` → 系统 `PATH` 环境变量。
- 提供 `DependencyStatusModel`，反馈工具到位状态（`IsAvailable`）、当前版本及可执行文件路径。
- 若缺少工具，支持在设置窗口的依赖检测卡中一键下载（`DownloadFfmpegCommand` / `DownloadYtDlpCommand`，工具已存在时对应下载按钮置灰）。
- 检测/下载失败记录到日志，启动时检测失败会弹出提示对话框。

### 进程控制与状态管理（暂停与取消机制）

- **单独取消：** 每个任务关联唯一的 `CancellationTokenSource`，点击取消立即取消 Token 并终止对应的后台 CLI 进程，清理临时缓存。
- **单独暂停 / 继续：**
  - 下载任务：记录已下载字节/状态，暂停时杀死 yt-dlp 进程；恢复时基于断点续传（`yt-dlp -c`）重新启动。
  - 转码任务：支持发送挂起/恢复进程信号，或取消后记录进度重新切片启动。
- **全局控制：** 下载页顶部提供「⏸ 暂停任务」「🗑 全部删除」「⬇ 开始下载」按钮，遍历所有正在运行的任务 ViewModel 批量触发暂停/删除/开始下载。「全部恢复」当前未提供。
- **状态栏反馈：** 左下角状态栏实时显示当前操作状态（正在获取分辨率、正在解析链接、正在下载、已添加 N 个任务等），通过 `StatusUpdateMessage` 事件驱动更新。

### 下载模块（Download）

- **交互与导入：** 支持在文本框粘贴 URL（每行一个），自动解析播放列表并展开为单视频列表。
- **进度解析：** 实时解析 yt-dlp 输出，正确显示下载进度百分比、已下载/总大小（KB/MB/GB）、下载速度（KB/s/MB/s/GB/s）。
- **格式选择与一键批量切换：**
  - 异步执行 `yt-dlp -F` 解析视频格式列表，绑定到每个任务项的 ComboBox 中。
  - 格式显示包含：分辨率、HDR 标记、视频编码、音频编码（如 `1080p (1920x1080) HDR [H.265] + AAC`）。
  - 顶栏支持「一键切换全局分辨率」，下拉含 16 项精确选择：4K/2K/1080P/720P 各含 120fps/60fps/30fps/24fps 四种帧率，加「最高可用」「仅音频」；下拉首项为占位项「批量设置分辨率」，选中不触发任何批量应用。
  - 任务行分辨率下拉使用占位文本「单独设置分辨率」，获取格式列表后不自动选中，需用户手动选择；格式列表按倒序展示。
  - 全局分辨率选择后自动更新所有待开始任务的选中格式，优先匹配目标帧率，再按「优先 HDR」复选框决定是否优先选择 HDR 格式（默认不勾选）。
  - 解析失败时弹出提示对话框显示具体错误信息，已有格式列表保持不变。
- **HDR 优先：** 每个任务支持「优先 HDR」复选框，在批量选择分辨率时，高度和帧率匹配后优先选择 HDR 格式（勾选时）或优先避开 HDR 格式（未勾选时）。
- **标题获取：** 添加任务时后台调用 `yt-dlp --skip-download --print title` 获取视频标题，取回后显示在任务卡标题行。失败时直接显示 yt-dlp 的错误信息而非备选标题。`SiteTitleResolver` 接收 `ILogger` 记录取标题日志。获取分辨率时若标题已存在则跳过重复获取。
- **并行下载：** 支持 1-8 个并行下载任务，通过下拉框选择，修改后自动保存。全部开始下载时，所有任务并发启动，由 `SemaphoreSlim` 控制实际同时下载数量不超过并行设置值，超过的排队等待。
- **错误信息：** 下载失败时捕获 yt-dlp 输出的错误行（包含 `error:` 的行），直接显示在任务状态栏中，替代笼统的"进程退出码"。
- **音频下载：** 纯音频格式不加 `--merge-output-format`（yt-dlp 不支持 m4a/mp3 等纯音频容器格式的合并参数）。
- **完成后打开文件夹：** 仅在所有任务全部完成（无运行中的下载/排队/解析任务）时打开输出文件夹，单个任务完成不触发。
- **解耦的站点适配器：** `Services/Download/Sites/` 目录下的每个 .cs 文件负责特定网站的解析与下载，未来新增网站只需新建文件并实现 `ISiteDownloader` 接口。注册顺序从通用到具体：YoutubeDownloader → BilibiliDownloader → DefaultDownloader（`FirstOrDefault` 按注册顺序匹配）。所有站点适配器注入 `IProcessRunner`、`IToolPathResolver`、`IDownloadConfiguration`、`ILogger`。
- **代理支持：** `--proxy` 参数注入到 `-F`、下载、续传、播放列表展开等所有 yt-dlp 调用中（HTTP(S) / SOCKS5）。支持绕过代理设置：Bilibili（默认绕过）和 YouTube 可单独选择是否使用代理。

### 视频转码模块（VideoConversion）

- **导入与勾选：** 支持文件选择器（多选）导入本地视频文件。每个任务列表项前自带 CheckBox，仅对勾选的任务执行转码。支持删除单个任务（取消按钮）和取消全部任务。
- **编码器手动选择：** 每个任务行带「编码器」下拉，启动时检测当前 ffmpeg 的 `-encoders` 实际支持项，仅列出与所选视频编码匹配的候选，显示格式如 `libx264 CPU（最好质量）` / `h264_nvenc GPU（NVIDIA）`；不再按优先级自动探测（ffmpeg 无 libx264 时可选 libopenh264，无 libx265 时可选 libkvazaar）。
- **输出去重：** 转码开始时若输出路径已存在，自动追加 `（1）`、`（2）`… 序号，不影响输入文件。
- **代理设置：** 必须支持 HTTP(S) 和 SOCKS5 代理的设置，以保障不同网络环境均可使用，并加入网络连通测试功能（设置窗口 → 网络与代理设置）。
- **视频编码矩阵：**
  - MP4 / MKV / MOV 支持：H.264、H.265、VP9、AV1
  - WEBM 仅支持：VP9、AV1（选择 WEBM 时自动禁用 H.264/H.265）
- **音频与格式互斥矩阵（UI 联动校验规则）：**
  - 选择 WEBM 容器时：音频编码仅允许 Opus 与 Vorbis，自动禁用 AAC、MP3、FLAC
  - 选择 MP4 容器时：音频编码允许 AAC、MP3、Opus，禁用 FLAC、Vorbis
  - 选择 MOV 容器时：音频编码允许 AAC、PCM，禁用 Opus、Vorbis
  - 选择 MKV 容器时：支持所有常见音频编码（AAC、MP3、Opus、FLAC、Vorbis、PCM）

### 窗口自适应与首次提示

- 设置窗口 / 关于窗口使用纯 `SizeToContent="WidthAndHeight"`，不设最小宽高；提示弹窗（MessageView）最小 150x100、内容与按钮居中；首次提示窗口（FirstRunView）`SizeToContent` + MinWidth=460 / MinHeight=120。
- 默认每次启动显示「使用提示」窗口（Title=软件提示），勾选「知道了」后才可点确定；勾选「启动时不再显示」将 `SuppressFirstRunTips=true` 写入 config.json，下次启动跳过。首次提示窗口的双复选框与确定按钮水平居中。

---

## 四、 交付要求

请严格按照上述文件目录结构，分文件提供完整的 C#（包含 App.axaml.cs 依赖注入配置、各个 Service、ViewModel、Model）与 AXAML 视图代码实现，确保符合 Clean Architecture，无强耦合，且完全支持异步调用规范。

**参考 axaml 文件：** 原始设计参考文件 `G:\VideoDownloader\1.axaml` 与 `G:\VideoDownloader\2.axaml` 当前不存在于仓库中。如需提供可另行添加。

**依赖工具：** 位于 `G:\VideoDownloader\tools\`（ffmpeg.exe、ffprobe.exe、yt-dlp.exe），实际运行时 ToolLocationProvider 优先检测应用目录下的 `.tools/` 目录，再检测系统 PATH。